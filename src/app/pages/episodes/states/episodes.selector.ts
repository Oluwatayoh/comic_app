// import { createSelector } from '@ngrx/store';
// import { Character } from 'src/app/models/character.model';
// import { CharacterState } from './characters.reducer';

// export const characterSelector = createSelector(
//   (state: CharacterState) => state.characters,
//   (charaters: ReadonlyArray<Character>) => charaters
// );

// export const character = createSelector(
//   characterSelector,
//   // selectRouteParams,
//   (charaters: ReadonlyArray<Character>) => charaters
// );
